{
  "name": "sigil1",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "SIGIL1 engine. Vite resolves the TypeScript constitutional layer (aether/, foundation/, runtime/, capabilities/) imported from engine.js.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0"
  }
}
